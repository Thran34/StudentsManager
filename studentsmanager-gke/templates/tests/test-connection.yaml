apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "studentsmanager-gke.fullname" . }}-test-connection"
  labels:
    {{- include "studentsmanager-gke.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "studentsmanager-gke.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
