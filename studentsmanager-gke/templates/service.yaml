apiVersion: v1
kind: Service
metadata:
  name: {{ include "studentsmanager-gke.fullname" . }}
  labels:
    {{- include "studentsmanager-gke.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "studentsmanager-gke.selectorLabels" . | nindent 4 }}
